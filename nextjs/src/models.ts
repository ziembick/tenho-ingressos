export type EventModel = {
    id: string;
    name: string;
    organization: string;
    date: string;
    price: string;
    rating: string;
    image_url: string;
    location: string; 
}

export type SpotModel = {
    id: string;
    name: string;
    status: string;
}